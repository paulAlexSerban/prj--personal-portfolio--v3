strict-peer-dependencies=false # warn on peer dep conflicts, do not fail
auto-install-peers=true        # install peer deps automatically
shamefully-hoist=false         # keep strict isolation (default, explicit here for clarity)