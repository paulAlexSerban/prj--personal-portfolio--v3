# (draft) Quiz UI — SUPERSEDED

> **Superseded by [ADR-008](../adr-008--quiz-web-app-architecture.md).**
> The "React + Vite" intent held; the location changed. The quiz UI is a CSR React
> 19 + Vite app (`frontend/apps/quiz-web-app`) whose presentation lives as
> Storybook-backed **blocks** in `shared/ui` (`./blocks`). Kept only for provenance.

See `shared/ui/readme.md` and `frontend/apps/quiz-web-app/readme.md`.
