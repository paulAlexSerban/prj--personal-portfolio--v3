---
import SiteFooter from '@/library/modules/SiteFooter.astro';
import SiteHeader from '@/library/modules/SiteHeader.astro';

import { assetUrl } from '@/lib/urls.ts';
import '@/styles/global.css';

interface Props {
    title: string;
    description?: string;
    ogTitle?: string;
    ogDescription?: string;
    canonicalUrl?: string;
    ogImage?: string;
}

const { title, description, ogTitle = title, ogDescription = description, canonicalUrl, ogImage = assetUrl('portrait.svg') } = Astro.props;

const pageDescription = description ?? 'Paul Serban — engineer, architect, selected works.';
const canonical = canonicalUrl ?? new URL(Astro.url.pathname, Astro.site).href;
const ogImageUrl = new URL(ogImage, Astro.site).href;
const sitemapUrl = assetUrl('sitemap-index.xml');
---

<!doctype html>
<html lang="en">
    <head>
        <script is:inline>
            (function () {
                try {
                    var t = localStorage.getItem('theme');
                    var dark = t === 'dark' || (!t && window.matchMedia('(prefers-color-scheme: dark)').matches);
                    document.documentElement.classList.toggle('dark', dark);
                } catch (e) {}
            })();
        </script>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>{title}</title>
        <meta name="description" content={pageDescription} />
        <meta property="og:title" content={ogTitle} />
        <meta property="og:description" content={ogDescription ?? pageDescription} />
        <meta property="og:type" content="website" />
        <meta property="og:url" content={canonical} />
        <meta property="og:image" content={ogImageUrl} />
        <link rel="canonical" href={canonical} />
        <link rel="sitemap" href={sitemapUrl} />
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400..900;1,400..900&display=swap" rel="stylesheet" />
        <slot name="head" />
    </head>
    <body class="body">
        <SiteHeader />
        <main class="root-box">
            <slot />
        </main>
        <SiteFooter />
    </body>
</html>
